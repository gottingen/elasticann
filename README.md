# elasticann
elasticann EA

# build

first install carbin package manager
```shell
pip install carbin
```
install dependencies

```shell
carbin install
```
then build

```shell
mkdir build
cd build
cmake ..
cmake -j 4
```